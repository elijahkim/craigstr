<h2>Regions</h2>

<ul>
  <% @regions.each do |region| %>
    <li><%= link_to region.name, region %></li>
  <% end %>
</ul>

<%= paginate @regions %>

<% if current_user.admin? %>
  <%= button_to "New Region", new_region_path, method: :get %>
<% end %>
